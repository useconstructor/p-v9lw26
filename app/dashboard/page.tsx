'use client'
import { useEffect,useState } from 'react'
import { useRouter } from 'next/navigation'
import { Activity,AppWindow,KeyRound,LogOut,ShieldCheck,Users } from 'lucide-react'
type User={email?:string;name?:string;workspace?:string}
export default function Dashboard(){const router=useRouter();const [user,setUser]=useState<User|null>(null);useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>{if(!d.user)router.replace('/login');else setUser(d.user)})},[router]);async function logout(){await fetch('/api/auth/logout',{method:'POST'});router.push('/login')}if(!user)return <div className="grid min-h-screen place-items-center">Loading workspace…</div>;return <main className="min-h-screen bg-brand-bg"><header className="flex h-16 items-center justify-between border-b bg-white px-5 lg:px-8"><span className="flex items-center gap-2 font-bold"><span className="grid size-8 place-items-center rounded-md bg-brand text-white"><ShieldCheck size={18}/></span>AuthGuard</span><button onClick={logout} className="flex items-center gap-2 text-sm font-semibold"><LogOut size={16}/> Sign out</button></header><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><p className="text-sm text-slate-500">{user.workspace} workspace</p><h1 className="mt-1 text-3xl font-bold">Welcome, {user.name||user.email}</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[[Users,'Active users','1'],[Activity,'Auth events','1'],[AppWindow,'Connected apps','0'],[KeyRound,'Active sessions','1']].map(([I,t,n])=>{const Icon=I as typeof Users;return <div className="rounded-md border bg-white p-5" key={t as string}><Icon className="text-brand"/><span className="mt-5 block text-sm text-slate-500">{t as string}</span><b className="text-3xl">{n as string}</b></div>})}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.5fr_1fr]"><section className="rounded-md border bg-white p-6"><h2 className="font-bold">Recent authentication activity</h2><div className="mt-5 flex items-center gap-4 border-t py-4"><span className="grid size-10 place-items-center rounded-full bg-emerald-100 text-emerald-700"><ShieldCheck/></span><div><b className="block text-sm">Successful account access</b><span className="text-xs text-slate-500">{user.email} · Current session</span></div></div></section><section className="rounded-md border bg-white p-6"><h2 className="font-bold">Profile</h2><dl className="mt-5 space-y-4 text-sm"><div><dt className="text-slate-500">Name</dt><dd className="font-semibold">{user.name||'Not set'}</dd></div><div><dt className="text-slate-500">Email</dt><dd className="font-semibold">{user.email}</dd></div><div><dt className="text-slate-500">Workspace</dt><dd className="font-semibold">{user.workspace}</dd></div></dl></section></div></div></main>}
