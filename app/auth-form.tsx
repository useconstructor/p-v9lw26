'use client'
import { FormEvent,useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import { Eye,EyeOff,ShieldCheck } from 'lucide-react'
export default function AuthForm({mode}:{mode:'login'|'register'}){const router=useRouter();const [form,setForm]=useState({name:'',workspace:'',email:'',password:''}),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await r.json();if(!r.ok)throw new Error(d.error);router.push('/dashboard');router.refresh()}catch(e){setError(e instanceof Error?e.message:'Something went wrong.')}finally{setLoading(false)}}return <main className="grid min-h-screen bg-brand-bg lg:grid-cols-2"><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><Link href="/" className="mb-10 flex items-center gap-2 font-bold"><span className="grid size-8 place-items-center rounded-md bg-brand text-white"><ShieldCheck size={18}/></span>AuthGuard</Link><h1 className="text-3xl font-bold">{mode==='login'?'Welcome back':'Start building securely'}</h1><p className="mt-2 text-slate-500">{mode==='login'?'Sign in to manage your users and apps.':'Create your workspace. No credit card required.'}</p><form onSubmit={submit} className="mt-8 space-y-4">{mode==='register'&&<><Field label="Your name" value={form.name} onChange={name=>setForm({...form,name})}/><Field label="Workspace name" value={form.workspace} onChange={workspace=>setForm({...form,workspace})} required/></>}<Field label="Work email" type="email" value={form.email} onChange={email=>setForm({...form,email})} required/><label className="block text-sm font-semibold">Password<div className="relative mt-2"><input minLength={8} required type={show?'text':'password'} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="w-full rounded-md border bg-white px-3 py-3 pr-11 outline-none focus:ring-2 focus:ring-blue-200"/><button type="button" aria-label="Toggle password visibility" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-slate-400">{show?<EyeOff size={20}/>:<Eye size={20}/>}</button></div></label>{error&&<p role="alert" className="rounded-md bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-md bg-brand py-3 font-semibold text-white disabled:opacity-60">{loading?'Please wait…':mode==='login'?'Sign in':'Create free account'}</button></form><p className="mt-6 text-center text-sm text-slate-500">{mode==='login'?"New to AuthGuard? ":'Already have an account? '}<Link className="font-semibold text-brand" href={mode==='login'?'/register':'/login'}>{mode==='login'?'Create an account':'Sign in'}</Link></p></div></section><aside className="hidden items-center bg-slate-950 p-16 text-white lg:flex"><div className="max-w-lg"><p className="text-3xl font-semibold leading-snug">“Authentication should be the strongest part of your product, not the slowest part of your roadmap.”</p><div className="mt-10 grid grid-cols-3 gap-4 text-sm"><div><b className="block text-2xl">99.9%</b><span className="text-slate-400">uptime SLA</span></div><div><b className="block text-2xl">&lt;200ms</b><span className="text-slate-400">login latency</span></div><div><b className="block text-2xl">24/7</b><span className="text-slate-400">monitoring</span></div></div></div></aside></main>}
function Field({label,value,onChange,type='text',required=false}:{label:string,value:string,onChange:(v:string)=>void,type?:string,required?:boolean}){return <label className="block text-sm font-semibold">{label}<input type={type} required={required} value={value} onChange={e=>onChange(e.target.value)} className="mt-2 w-full rounded-md border bg-white px-3 py-3 outline-none focus:ring-2 focus:ring-blue-200"/></label>}
