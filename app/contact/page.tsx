'use client'
import { FormEvent,useState } from 'react'
import Link from 'next/link'
import { ArrowLeft,ShieldCheck } from 'lucide-react'
export default function Contact(){const [form,setForm]=useState({name:'',email:'',company:'',message:''}),[loading,setLoading]=useState(false),[success,setSuccess]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const base=process.env.NEXT_PUBLIC_CONSTRUCTOR_API,id=process.env.NEXT_PUBLIC_PROJECT_ID;if(!base||!id)throw new Error('Form service is not configured.');const r=await fetch(`${base}/v1/forms/${id}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(!r.ok)throw new Error('Unable to send your message. Please try again.');setSuccess(true)}catch(e){setError(e instanceof Error?e.message:'Unable to send your message.')}finally{setLoading(false)}}return <main className="min-h-screen bg-brand-bg px-5 py-10"><div className="mx-auto max-w-5xl"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold"><ArrowLeft size={16}/> Back to AuthGuard</Link><div className="mt-14 grid overflow-hidden rounded-lg border bg-white shadow-xl lg:grid-cols-2"><div className="bg-slate-950 p-8 text-white sm:p-12"><span className="grid size-10 place-items-center rounded-md bg-brand"><ShieldCheck/></span><h1 className="mt-8 text-4xl font-bold">Let’s design your secure path forward.</h1><p className="mt-4 leading-7 text-slate-400">Tell our team about your user volume, authentication needs, and migration goals. We’ll help map the right approach.</p></div><div className="p-8 sm:p-12">{success?<div className="rounded-md bg-emerald-50 p-5 font-semibold text-emerald-800">✓ Message sent — we’ll contact you soon</div>:<form onSubmit={submit} className="space-y-5">{[['Name','name'],['Work email','email'],['Company','company']].map(([label,key])=><label className="block text-sm font-semibold" key={key}>{label}<input required type={key==='email'?'email':'text'} value={form[key as keyof typeof form]} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-2 w-full rounded-md border px-3 py-3 outline-none focus:ring-2 focus:ring-blue-200"/></label>)}<label className="block text-sm font-semibold">How can we help?<textarea required rows={5} value={form.message} onChange={e=>setForm({...form,message:e.target.value})} className="mt-2 w-full rounded-md border px-3 py-3 outline-none focus:ring-2 focus:ring-blue-200"/></label>{error&&<p className="text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-md bg-brand py-3 font-semibold text-white disabled:opacity-60">{loading?'Sending…':'Talk to sales'}</button></form>}</div></div></div></main>}
